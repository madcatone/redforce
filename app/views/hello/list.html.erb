<div class="jumbotron">
        <h1 class="text-center">你好，歡迎來到這裡！</h1>
        <p class="text-center">這是一個用Ruby on Rails打造的網站！</p>
        <p class="text-center">堪稱(底細)人力資源庫</p>

        <p class="text-center"><a href="groups" class="btn btn-success btn-lg" role="button">更多訊息 &raquo;</a></p>
      </div>

<div class="span12">
    
    <div class="group">
        <%= link_to("New group", new_group_path , :class => "btn btn-mini btn-primary pull-left" ) %>
    </div>
 
    <h3>人力資源庫</h3>
    <table class="table">
        <thead>
            <tr>
                <td> # </td>
                <td> ID </td>
                <td> 介紹 </td>
            </tr>
        </thead>

    <tbody>
        <% @groups.each do |group| %>
            <tr>
                <td> # </td>
                <td> <%= link_to(group.title, group_path(group)) %> </td>
                <td> <%= group.description %> </td>
                <td> <%= link_to("Edit", edit_group_path(group), :class => "btn btn-mini" ) %><%= link_to("Delete", group_path(group), :class => "btn btn-mini", :method => :delete, :confirm => "Are you sure?" ) %></td>
            </tr>
        <% end %>
    </tbody>
    </table>

</div>